<!DOCTYPE html>
<html>
  <head>
    <title><%= type %></title>
    <link rel="stylesheet" href="/stylesheets/newItem.css" />
    <meta charset="utf-8" />
  </head>
  <body>
    <header>
      <nav>
        <ul>
          <li><a href="/">← Voltar para Home</a></li>
          <li><a href="/products">Produtos</a></li>
          <li><a href="/clients">Clientes</a></li>
        </ul>
      </nav>
    </header>
    <main class="container">
      <h1><%= title %></h1>
      <p>Preencha os dados abaixo para salvar o cliente.</p>
      <form action="<%= action %>" method="POST" data-form>
        <label for="nome">Nome:</label>
        <input
        type="text"
        name="nome"
        id="nome"
        value="<%=result.nome%>"
        required
        />
        
        <%if(type == "clients") {%>
          <label for="idade">Idade:</label>
          <input
          type="number"
          name="idade"
          id="idade"
          value="<%= result.idade %>"
          required
          />
          
          <label for="email">Email:</label>
          <input type="email" name="email" id="email"  value="<%=result.email%>"/>
          
          <label for="tel">Telefone:</label>
          <input type="tel" name="tel" id="tel" value="<%= result.telefone %>"/>
          <%}else if(type==="products"){%>
            <label for="preco">Preço:</label>
            <input type="number" name="preco" id="preco" min="1" step="any" required />
            
            <label min="1" for="stock">Em stock:</label>
            <input
            type="number"
            name="stock"
            id="stock"
            required
            />
            <%}%>
            
            <div class="form-actions">
              <a href="/">Cancelar</a>
              <button type="submit" />Salvar</button>
              <button type="submit" data-another>Salvar e Criar outro</button>
            </div>
          </form>
          <script src="/js/newItem.js"></script>
    </main>
  </body>
</html>
